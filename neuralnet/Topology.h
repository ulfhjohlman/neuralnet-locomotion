#pragma once

#include "NeuralNet.h"

/// <summary>
/// Defines a general topology, empty for now.
/// </summary>
class Topology 
{
public:
	Topology() = default;
	virtual ~Topology() = default;
private:
};
